import PipelineTreeDataProvider from './pipeline-tree-data-provider';

describe('PipelineTreeDataProvider', () => {
  it('can be instantiated', () => {
    expect(() => new PipelineTreeDataProvider()).not.toThrowError();
  });
});
