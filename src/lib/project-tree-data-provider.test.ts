import ProjectTreeDataProvider from './project-tree-data-provider';

describe('ProjectTreeDataProvider', () => {
  it('can be instantiated', () => {
    expect(() => new ProjectTreeDataProvider()).not.toThrowError();
  });
});
